spring:
  application:
    name: ecom-app
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test

logging:
  level:
    org.hibernate.SQL: DEBUG           # see SQL in dev
    org.hibernate.orm.jdbc.bind: TRACE # see bind params